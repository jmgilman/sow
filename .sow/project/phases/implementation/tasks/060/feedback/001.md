# Review - Iteration 1

## Summary

Reviewed the consumer migration to libs/git:

**Files modified (13):**
- `cli/go.mod` - Added libs/git dependency
- `cli/internal/sow/context.go` - Updated to use libs/git types
- `cli/cmd/issue/list.go`, `show.go`, `check.go` - Use git.NewGitHubClient
- `cli/cmd/project/wizard_state.go`, `wizard_helpers.go`, `shared.go`
- Test files: `shared_test.go`, `wizard_helpers_test.go`, `wizard_integration_test.go`, `wizard_state_test.go`

**Files deleted (10):**
- `cli/internal/sow/git.go`
- `cli/internal/sow/github_*.go` and tests
- `cli/internal/sow/worktree.go` and test

## Assessment

**PASS**

## Feedback

All acceptance criteria met:

1. ✅ Imports updated: All consumers use `libs/git` instead of `cli/internal/sow`
2. ✅ Types migrated: Issue, LinkedBranch, Label, GitHubClient, errors
3. ✅ Functions migrated: WorktreePath, EnsureWorktree (4-param), CheckUncommittedChanges
4. ✅ Mocks migrated: sow.MockGitHub → mocks.GitHubClientMock
5. ✅ Old files deleted: All git/github files removed from cli/internal/sow
6. ✅ Build passes: `go build ./...` succeeds
7. ✅ Tests pass: All 24 packages pass
8. ✅ Linting clean: 0 issues

Key migration changes correctly applied:
- `sow.EnsureWorktree(ctx, path, branch)` → `git.EnsureWorktree(ctx.Git(), ctx.RepoRoot(), path, branch)`
- `sow.CheckUncommittedChanges(ctx)` → `git.CheckUncommittedChanges(ctx.Git())`

Note: Task 070 (cleanup) may be partially completed by this task since old files were already deleted.

## Next Steps

Task complete. Moving to task 070 (cleanup and verification).
