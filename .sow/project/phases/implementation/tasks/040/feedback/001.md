# Review - Iteration 1

## Summary
Reviewed consumer migration from `cli/internal/exec` to `github.com/jmgilman/sow/libs/exec`:
- 8 consumer files updated
- cli/go.mod updated with dependency and replace directive

## Assessment
**PASS**

## Feedback
All acceptance criteria met:

1. **cli/go.mod updated:**
   - Added `github.com/jmgilman/sow/libs/exec v0.0.0` to require block
   - Added `replace github.com/jmgilman/sow/libs/exec => ../libs/exec` directive

2. **All 8 consumer files updated:**
   - `cli/internal/sow/github_cli.go` - import path updated
   - `cli/internal/sow/github_cli_test.go` - updated to use `mocks.ExecutorMock`
   - `cli/internal/sow/github_factory.go` - import + `NewLocalExecutor` constructor
   - `cli/cmd/project/wizard_state.go` - removed alias, updated import and constructor
   - `cli/cmd/project/shared.go` - handled `os/exec` conflict with alias, updated constructor
   - `cli/cmd/issue/show.go` - import + constructor + fixed deprecated `NewGitHub` → `NewGitHubCLI`
   - `cli/cmd/issue/list.go` - same as show.go
   - `cli/cmd/issue/check.go` - same as show.go

3. **Constructor changes:**
   - All `exec.NewLocal(...)` changed to `exec.NewLocalExecutor(...)`

4. **Mock migration:**
   - `&exec.MockExecutor{}` changed to `&mocks.ExecutorMock{}`
   - Import added: `github.com/jmgilman/sow/libs/exec/mocks`

5. **Import organization:**
   - Import groups reorganized per STYLE.md (stdlib, external, internal)

**Verification:**
- `cd cli && go build ./...` passes
- `cd cli && go test ./...` passes (24 packages)
- `golangci-lint run` passes (per log.md)

## Next Steps
Task complete. Proceeding to task 050 (cleanup and final verification).
